<!-- Form 03 (append) -->
<form id="form-03">
    <div class="card border-primary mb-3">
        <h2 class="card-header bg-primary border-primary h5">
            Upload a File
        </h2>
        <div class="card-body">
            <div class="mb-4">
                <label for="appendFile" class="form-label">Supplemental File</label>
                <input class="form-control" type="file" id="appendFile" name="appendFile">
            </div>
        </div>
        <div class="card-footer border-top-none p-3">
            <button class="btn btn-primary" type="submit">Submit</button>
            <a class="btn btn-outline-secondary ms-2" href=".">Exit without submitting</a>
        </div>
    </div>
</form>
<!-- /#form-03 (append) -->
